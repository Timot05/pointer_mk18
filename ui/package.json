{
  "name": "pointer-mk18-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "dotnet fable watch src --outDir src-gen --run vite",
    "build:fs": "dotnet fable src --outDir src-gen",
    "build": "dotnet fable src --outDir src-gen && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@carbon/icons": "^11.78.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
