{
  "name": "zig-interval-viewer",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "zig build -Doptimize=ReleaseFast",
    "dev": "npm run build:wasm && vite",
    "build": "npm run build:wasm && vite build"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.69",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
